# 格式转换工具 - 启动说明

## 第一批次完成内容 ✅

### 后端 (backend/)
- ✅ FastAPI用户认证系统（注册/登录/JWT）
- ✅ 数据库SQLite（用户表、转换记录表）
- ✅ 文件转换API（Word转PDF、图片转PDF、PDF合并）
- ✅ bcrypt密码加密

### 前端 (frontend/)
- ✅ Vue3 + Vite项目结构
- ✅ 登录/注册页面（AuthPage.vue）
- ✅ 首页转换功能（HomePage.vue）
- ✅ 认证API服务（api.ts）

## 一键启动（推荐）✅

直接双击项目根目录下的批处理文件即可：

| 文件 | 功能 |
|------|------|
| `一键启动.bat` | 启动前端+后端（首次使用） |
| `重启服务.bat` | 重启前端+后端（修改代码后） |
| `停止服务.bat` | 停止所有服务 |

## 手动启动步骤

### 1. 启动后端
```bash
cd backend
python main.py
```
后端运行在: http://localhost:8000

### 2. 启动前端（新窗口）
```bash
cd frontend
npm install
npm run dev
```
前端运行在: http://localhost:5173

## API接口

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/auth/register | POST | 用户注册 |
| /api/auth/login | POST | 用户登录 |
| /api/auth/me | GET | 获取当前用户 |
| /api/health | GET | 健康检查 |
| /api/convert/word-to-pdf | POST | Word转PDF |
| /api/convert/image-to-pdf | POST | 图片转PDF |
| /api/convert/pdf-merge | POST | PDF合并 |

## 注意事项

1. 首次使用需要先启动后端初始化数据库
2. 前端会自动检测后端连接状态
3. 注册用户后会自动登录

## 默认账户

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | Admin@123456 |
| 普通用户 | testuser | Test@123456 |

> 💡 如需重新初始化账户，运行: `cd backend && python init_db.py`
